{
  "name": "cloudwatch-mcp-server",
  "version": "1.0.0",
  "description": "Local MCP server connecting AI agents to AWS CloudWatch Logs via SSO",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch-logs": "^3",
    "@aws-sdk/credential-providers": "^3",
    "@modelcontextprotocol/sdk": "^1",
    "zod": "^4"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  }
}
